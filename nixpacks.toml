[phases.setup]
nixPkgs = ["python312"]

[phases.install]
cmds = ["python -m venv /app/.venv && /app/.venv/bin/pip install -r requirements.txt"]

[start]
cmd = "/app/.venv/bin/uvicorn api.main:app --host 0.0.0.0 --port ${PORT:-8000}"
